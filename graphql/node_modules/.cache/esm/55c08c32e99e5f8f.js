let dotenv,express,server,mongoose;_e99‍.w("dotenv",[["default",["dotenv"],function(v){dotenv=v}]]);_e99‍.w("express",[["default",["express"],function(v){express=v}]]);_e99‍.w("./server",[["default",["server"],function(v){server=v}]]);_e99‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);
dotenv.config();




const { db_username, db_password, DB_name, PORT } = process.env;

const createServer = async() => {
    try {
        await mongoose.connect(
            // replace 'url' with your mongodb url
            `mongodb+srv://${db_username}:${db_password}@qraphqlbasic-e7zl1.mongodb.net/${DB_name}?retryWrites=true&w=majority`, { useUnifiedTopology: true }
        );

        const app = express();

        server.applyMiddleware({ app });

        app.listen({ port: PORT }, () =>
            console.log(
                `🚀 Server ready at http://localhost:${PORT}${server.graphqlPath}`
            )
        );
    } catch (error) {
        _e99‍.g.console.log(error);
    }
};
createServer();